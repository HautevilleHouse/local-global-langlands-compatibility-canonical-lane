# EG1 Public Note

Gate: `LGC_G1`
Constant: `kappa_compatibility`

This note isolates the active projected-response package. The projected response operator is `E_tau = Pi_resp L_tau Pi_resp`. The super-repo closes this gate when the declared admissible lattice carries a uniform positive floor on the projected response sector.
