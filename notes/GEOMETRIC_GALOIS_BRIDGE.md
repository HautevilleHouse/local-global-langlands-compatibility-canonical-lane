# Geometric-Galois Bridge

This note records the mainstream bridge for the canonical-lane package.

Mainstream precursors:
- compatible Galois representations, local Langlands parameters, and automorphic packets provide the mainstream reciprocity language underlying the lane.
- the lane adds admissible-carrier, defect-ledger, and remainder-tracking structure on top of that standard reciprocity package.

Problem-side translation:
- `P_mc` projects the visible reciprocity object to the admissible sector `Y_mc^LGC` tracked by the lane.
- `Q_rem := I - P_mc` records the explicit remainder `R_LGC` carried by the defect and coherence ledgers.
- the manuscript closes on `Y_mc^LGC`; any stronger external consequence must control `R_LGC` rather than dropping it.

Reviewer use:
- translate the transport language into a decomposition between the admissible sector and the unresolved complement,
- compare the bridge package to the standard arithmetic/geometric objects rather than reading the lane as disconnected notation.
